# mean-variance-std-calculator
A Python project that calculates the mean, variance, standard deviation, maximum, minimum, and sum of a 3×3 matrix. The input is a list of 9 numbers, and the output is a dictionary with statistics for rows, columns, and the entire matrix using NumPy.
